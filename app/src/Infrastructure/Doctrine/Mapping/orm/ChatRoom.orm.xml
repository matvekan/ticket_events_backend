<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="App\Domain\Entity\ChatRoom" table="chat_rooms">
        <id name="id" type="chat_room_id">
            <generator strategy="NONE"/>
        </id>

        <!-- Cross-aggregate reference by ID only. -->
        <field name="userId" column="user_id" type="user_id"/>
        <field name="createdAt" type="datetime_immutable"/>

        <unique-constraints>
            <unique-constraint name="uniq_chat_rooms_user" columns="user_id"/>
        </unique-constraints>
    </entity>
</doctrine-mapping>
